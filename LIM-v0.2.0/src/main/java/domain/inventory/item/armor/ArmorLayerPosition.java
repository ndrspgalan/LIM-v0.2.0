package domain.inventory.item.armor;

/** Orden físico de estratificación corporal. HEAD usa su taxonomía funcional propia mediante HeadLayer. */
public enum ArmorLayerPosition {
    INNER,
    MIDDLE,
    OUTER,
    UNSPECIFIED
}
