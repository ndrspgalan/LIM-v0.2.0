package domain.combat;

/** Variante cinética elegida al liberar una preparación cargada personalizada. */
public enum ChargedAttackReleaseVariant {
    STANDARD,
    LEFT_TO_RIGHT,
    RIGHT_TO_LEFT,
    FORWARD_DRAW
}
