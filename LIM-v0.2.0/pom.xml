<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>io.lim</groupId>
    <artifactId>lim-vertical-slice</artifactId>
    <version>0.2.0</version>
    <name>LIM Vertical Slice</name>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <junit.version>5.11.4</junit.version>
        <lim.qa.propertySeed>21472694636982322</lim.qa.propertySeed>
        <lim.qa.fuzzSeed>5567663280820933953</lim.qa.fuzzSeed>
        <lim.qa.propertyCases>10000</lim.qa.propertyCases>
        <lim.qa.fuzzCases>25000</lim.qa.fuzzCases>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <showWarnings>true</showWarnings>
                    <compilerArgs>
                        <arg>-Xlint:deprecation</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
                <configuration>
                    <useModulePath>false</useModulePath>
                    <includes>
                        <include>**/*Test.class</include>
                        <include>**/*Verification.class</include>
                    </includes>
                    <parallel>none</parallel>
                    <trimStackTrace>false</trimStackTrace>
                    <systemPropertyVariables>
                        <lim.qa.propertySeed>${lim.qa.propertySeed}</lim.qa.propertySeed>
                        <lim.qa.fuzzSeed>${lim.qa.fuzzSeed}</lim.qa.fuzzSeed>
                        <lim.qa.propertyCases>${lim.qa.propertyCases}</lim.qa.propertyCases>
                        <lim.qa.fuzzCases>${lim.qa.fuzzCases}</lim.qa.fuzzCases>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>Main</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
    <profiles>
        <profile>
            <id>stress-million</id>
            <properties>
                <lim.qa.fuzzCases>1000000</lim.qa.fuzzCases>
            </properties>
        </profile>
    </profiles>

</project>
